{
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.25.0",
    "@typescript-eslint/parser": "^5.25.0",
    "eslint": "^8.15.0",
    "eslint-plugin-react": "^7.30.0",
    "prettier": "2.6.2"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "format": "yarn prettier --write .",
    "build": "echo TODO",
    "run": "yarn build && cd build && node"
  }
}
